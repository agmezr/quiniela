/**
 * This file contains the configuration for the api, some examples are the
 * oauth config if using google or session secret *
 */
var config = {
  positionHidden: false // if true only the id user in session position will show
  // sessionSecret: "sessionSecret",
  // googleOAuthId: "<id>",
  // googleOAuthSecret: "<secret>"
}

module.exports = config;
